extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href="/new") Add a message
  div 
    each message in messages
     h2 #{message.user}
     p #{message.added}
     p #{message.text}

     
